# linux-userspace-builder
A tool to build a linux userspace and kernel itself if selected so.


## Note!
Due to the limited things for me, this will be building a limited amount of packages. Also it compiles a custom gcc 15 build that can slow down the build process a LOT.